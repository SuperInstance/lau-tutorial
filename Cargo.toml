[package]
name = "lau-tutorial"
version = "0.1.0"
edition = "2021"
description = "Interactive tutorial system — teaches git and AI concepts through gameplay"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
